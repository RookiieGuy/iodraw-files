```mermaid
flowchart TD
    %% 整体系统流程
    A[数据输入]
    A --> B[效能自动建模模块]
    B --> C[生成与优化效能模型]
    C --> D[效能高效预测模块]
    D --> E[高效精准预测结果]
    
    %% 效能自动建模模块细节（GCN-LSTM + 元学习）
    subgraph Modeling [效能自动建模 (GCN-LSTM + 元学习)]
      M1[图结构数据<br>(指标间关系)]
      M2[时间序列效能数据]
      M1 --> M3[GCN提取图结构特征]
      M2 --> M4[LSTM：捕捉时间序列特征]
      M3 & M4 --> M5[特征融合]
      M5 --> M6[元学习：自适应调整模型]
      M6 --> M7[生成与优化效能模型]
    end

    %% 效能高效预测模块细节（Transformer + 模型压缩）
    subgraph Prediction [效能高效预测 (Transformer + 模型压缩)]
      P1[Transformer模型<br>(并行计算、长序列处理)]
      P1 --> P2[捕捉长期依赖关系]
      P2 --> P3[应对超大指标体系及计算量问题]
      P3 --> P4[模型压缩：知识蒸馏 & 剪枝]
      P4 --> P5[降低计算复杂度]
    end

    %% 连接自动建模与预测模块
    C ---|模型输出| P1

```